// Your code here
function createEmployeeRecord(array) {
    const employee = {
        firstName: (array[0]),
        familyName: (array[1]),
        title: (array[2]),
        payPerHour: (array[3])
    }
}
